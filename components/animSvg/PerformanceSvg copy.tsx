"use client"

import React from 'react';
import { motion } from 'framer-motion';

interface PerformanceSvgProps {
  active: boolean;
  className?: string;
}

const PerformanceSvg: React.FC<PerformanceSvgProps> = ({ active, className }) => {
  const [animationCompleted, setAnimationCompleted] = React.useState(false);

  return (

      
  );
};

export default PerformanceSvg;
